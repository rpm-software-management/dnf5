``--download-sort=size``
    | Sort the packages to download by their download size, smallest first.

``--reverse``
    | To be used together with ``--download-sort``. Reverses the sort order, e.g. ``--download-sort=size --reverse`` downloads the largest packages first.
