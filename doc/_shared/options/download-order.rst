``--download-sort=size``
    | Sort the packages to download by their download size, smallest first.

``--download-reverse``
    | Reverses the order packages are downloaded in. Combined with ``--download-sort``, e.g. ``--download-sort=size --download-reverse`` downloads the largest packages first. Used on its own, it reverses the default download order.
